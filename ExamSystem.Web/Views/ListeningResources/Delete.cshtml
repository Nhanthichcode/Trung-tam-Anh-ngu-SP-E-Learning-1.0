@model ExamSystem.Core.Entities.ListeningResource

@{
    ViewData["Title"] = "Xóa File Nghe";
}

<div class="alert alert-danger" role="alert">
    <h4 class="alert-heading">Cảnh báo!</h4>
    <p>Bạn có chắc chắn muốn xóa bài nghe này không? Hành động này sẽ <strong>xóa vĩnh viễn file âm thanh</strong> khỏi hệ thống và không thể khôi phục.</p>
</div>

<div class="card shadow-sm">
    <div class="card-header bg-danger text-white">
        Xác nhận xóa
    </div>
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3">Tiêu đề bài nghe:</dt>
            <dd class="col-sm-9 fw-bold">@Model.Title</dd>

            <dt class="col-sm-3">Nghe thử:</dt>
            <dd class="col-sm-9">
                @if (!string.IsNullOrEmpty(Model.AudioUrl))
                {
                    <audio controls src="@Model.AudioUrl"></audio>
                }
                else
                {
                    <span class="text-muted">Không có file</span>
                }
            </dd>

            <dt class="col-sm-3">Transcript:</dt>
            <dd class="col-sm-9 text-muted">
                @(Model.Transcript?.Length > 100 ? Model.Transcript.Substring(0, 100) + "..." : Model.Transcript)
            </dd>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />

            <div class="mt-4">
                <button type="submit" class="btn btn-danger">Xóa vĩnh viễn</button>
                <a asp-action="Index" class="btn btn-secondary">Hủy bỏ</a>
            </div>
        </form>
    </div>
</div>