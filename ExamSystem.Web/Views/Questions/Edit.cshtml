@model ExamSystem.Web.Models.QuestionViewModel
@using ExamSystem.Core.Enums

@{
    ViewData["Title"] = "Sửa câu hỏi";
}

<div class="container mt-4">
    <div class="card border-warning">
        <div class="card-header bg-warning">
            <h4><i class="fa fa-pencil"></i> Sửa câu hỏi #@Model.Id (@Model.Type)</h4>
        </div>
        <div class="card-body">

            <form asp-action="Edit" enctype="multipart/form-data">
                <input type="hidden" asp-for="Id" />
                <input type="hidden" asp-for="Type" id="QuestionType" />
                <input type="hidden" asp-for="Explaination" /> @* Giữ lại Explaination *@

                <div class="row">
                    <div class="col-md-8">
                        <div class="form-group mb-3">
                            <label class="fw-bold">Nội dung / Đề bài</label>
                            <textarea asp-for="Content" class="form-control" rows="5"></textarea>
                        </div>

                        @* HIỂN THỊ OPTIONS TỪ VIEWMODEL *@
                        @if (Model.Type != QuestionType.Writing && Model.Type != QuestionType.Speaking)
                        {
                            <div class="card border-secondary mb-3">
                                <div class="card-header bg-secondary text-white">Đáp án (Chỉ cho trắc nghiệm)</div>
                                <div class="card-body">
                                    <div class="row g-2">
                                        @* Sử dụng các trường OptionX của ViewModel, đã được map ngược từ Controller *@
                                        <div class="col-6"><div class="input-group"><span class="input-group-text fw-bold">A</span><input name="OptionA" class="form-control" placeholder="A" value="@Model.OptionA" /></div></div>
                                        <div class="col-6"><div class="input-group"><span class="input-group-text fw-bold">B</span><input name="OptionB" class="form-control" placeholder="B" value="@Model.OptionB" /></div></div>
                                        <div class="col-6"><div class="input-group"><span class="input-group-text fw-bold">C</span><input name="OptionC" class="form-control" placeholder="C" value="@Model.OptionC" /></div></div>
                                        <div class="col-6"><div class="input-group"><span class="input-group-text fw-bold">D</span><input name="OptionD" class="form-control" placeholder="D" value="@Model.OptionD" /></div></div>
                                    </div>
                                    <div class="mt-2">
                                        <label>Đáp án đúng (A/B/C/D):</label>
                                        <input name="CorrectAnswer" class="form-control w-25 text-uppercase" value="@Model.CorrectAnswer" />
                                    </div>
                                </div>
                            </div>
                        }
                    </div>

                    <div class="col-md-4">
                        <div class="mb-3">
                            <label class="fw-bold">Độ khó</label>
                            <select asp-for="Level" class="form-select">
                                <option value="1">1</option>
                                <option value="2">2</option>
                                <option value="3">3</option>
                                <option value="4">4</option>
                                <option value="5">5</option>
                            </select>
                        </div>

                        @* HIỂN THỊ MEDIA (Giữ nguyên) *@
                        @if (Model.Type == QuestionType.Speaking || Model.Type == QuestionType.Listening || !string.IsNullOrEmpty(Model.MediaUrl))
                        {
                            <div class="card bg-light">
                                <div class="card-header">Media (Ảnh/Audio)</div>
                                <div class="card-body text-center">
                                    @if (!string.IsNullOrEmpty(Model.MediaUrl))
                                    {
                                        <div class="mb-2">
                                            @if (Model.MediaUrl.EndsWith("mp3") || Model.MediaUrl.EndsWith("wav"))
                                            {
                                                <audio controls src="@Model.MediaUrl" class="w-100"></audio>
                                            }
                                            else
                                            {
                                                <img src="@Model.MediaUrl" class="img-fluid" style="max-height:150px" />
                                            }
                                        </div>
                                    }
                                    <input type="file" name="fileUpload" class="form-control" />
                                    <small class="text-muted">Chọn file mới để thay thế.</small>
                                </div>
                            </div>
                        }
                    </div>
                </div>

                <div class="text-center mt-3">
                    <button type="submit" class="btn btn-warning px-5">CẬP NHẬT</button>
                </div>
            </form>
        </div>
    </div>
</div>