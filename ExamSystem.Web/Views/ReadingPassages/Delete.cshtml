@model ExamSystem.Core.Entities.ReadingPassage

@{
    ViewData["Title"] = "Xác nhận xóa Bài đọc";
}

<div class="container mt-5">
    <div class="card border-danger shadow-lg">
        <div class="card-header bg-danger text-white">
            <h4 class="m-0"><i class="fa fa-exclamation-triangle"></i> CẢNH BÁO XÓA BÀI ĐỌC</h4>
        </div>
        <div class="card-body">

            <p class="fw-bold text-danger fs-5">Bạn có chắc chắn muốn xóa Bài đọc này không?</p>
            <p class="text-muted">
                <i class="fa fa-warning"></i>
                Việc xóa bài đọc này sẽ tự động **XÓA TẤT CẢ CÁC CÂU HỎI** đang sử dụng nó.
                Đây là hành động không thể hoàn tác.
            </p>

            <hr class="text-danger" />

            <dl class="row bg-light p-3 rounded">
                <dt class="col-sm-3 fw-bold">ID</dt>
                <dd class="col-sm-9">@Model.Id</dd>

                <dt class="col-sm-3 fw-bold">Tiêu đề</dt>
                <dd class="col-sm-9">@Model.Title</dd>

                <dt class="col-sm-3 fw-bold">Nội dung (Trích đoạn)</dt>
                <dd class="col-sm-9">
                    @(Model.Content.Length > 100 ? Model.Content.Substring(0, 100) + "..." : Model.Content)
                </dd>
            </dl>

            <form asp-action="Delete">
                <input type="hidden" asp-for="Id" />
                <div class="d-flex justify-content-between mt-4">
                    <a asp-action="Index" class="btn btn-secondary"><i class="fa fa-arrow-left"></i> Hủy và Quay lại</a>
                    <button type="submit" class="btn btn-danger px-5 shadow">
                        <i class="fa fa-trash"></i> Xác nhận XÓA
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>