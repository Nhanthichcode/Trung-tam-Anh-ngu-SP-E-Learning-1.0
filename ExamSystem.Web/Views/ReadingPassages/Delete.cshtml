@model ExamSystem.Core.Entities.Exam

@{
    ViewData["Title"] = "Xóa Đề thi";
}

<div class="alert alert-danger" role="alert">
    <h4 class="alert-heading"><i class="bi bi-exclamation-triangle-fill"></i> Cảnh báo!</h4>
    <p>Bạn có chắc chắn muốn xóa đề thi này không? Hành động này <strong>không thể hoàn tác</strong> và có thể xóa luôn các câu hỏi liên quan.</p>
</div>

<div class="card">
    <div class="card-body">
        <h5 class="card-title text-danger">Thông tin đề thi sẽ xóa:</h5>
        <hr />
        <dl class="row">
            <dt class="col-sm-3">Tên đề thi</dt>
            <dd class="col-sm-9 fw-bold">@Model.Title</dd>

            <dt class="col-sm-3">Mô tả</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.Description)</dd>

            <dt class="col-sm-3">Thời lượng</dt>
            <dd class="col-sm-9">@Model.DurationMinutes phút</dd>

            <dt class="col-sm-3">Trạng thái</dt>
            <dd class="col-sm-9">
                @(Model.IsActive ? "Đang mở" : "Đã đóng")
            </dd>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <div class="d-flex gap-2 mt-4">
                <button type="submit" class="btn btn-danger">Xác nhận Xóa</button>
                <a asp-action="Index" class="btn btn-secondary">Quay lại</a>
            </div>
        </form>
    </div>
</div>