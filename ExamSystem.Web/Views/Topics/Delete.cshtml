@model ExamSystem.Core.Entities.Topic

@{
    ViewData["Title"] = "Xác nhận xóa";
}

<div class="container mt-5">
    <div class="card border-danger shadow-lg">
        <div class="card-header bg-danger text-white">
            <h4 class="m-0"><i class="fa fa-exclamation-triangle"></i> CẢNH BÁO XÓA CHỦ ĐỀ</h4>
        </div>
        <div class="card-body">

            <p class="fw-bold text-danger fs-5">Bạn có chắc chắn muốn xóa Chủ đề này không?</p>
            <p class="text-muted">Việc xóa chủ đề sẽ không xóa các câu hỏi đã được gán, nhưng bạn sẽ không thể lọc theo chủ đề này nữa.</p>

            <hr class="text-danger" />

            <dl class="row bg-light p-3 rounded">
                <dt class="col-sm-3 fw-bold">ID</dt>
                <dd class="col-sm-9">@Model.Id</dd>

                <dt class="col-sm-3 fw-bold">Tên Chủ đề</dt>
                <dd class="col-sm-9">@Model.Name</dd>

                <dt class="col-sm-3 fw-bold">Mô tả</dt>
                <dd class="col-sm-9">@Model.Description</dd>
            </dl>

            <form asp-action="Delete">
                <input type="hidden" asp-for="Id" />
                <div class="d-flex justify-content-between mt-4">
                    <a asp-action="Index" class="btn btn-secondary"><i class="fa fa-arrow-left"></i> Hủy và Quay lại</a>
                    <button type="submit" class="btn btn-danger px-5 shadow">
                        <i class="fa fa-trash"></i> Xác nhận XÓA
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>